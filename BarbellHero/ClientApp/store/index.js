import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

// TYPES

// STATE
const state = {
    counter: 0
}

// MUTATIONS
const mutations = {
}

// ACTIONS
const actions = ({
})

export default new Vuex.Store({
    state,
    mutations,
    actions
});
